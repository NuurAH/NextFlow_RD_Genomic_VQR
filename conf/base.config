/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    nf-core/chipseq Nextflow base config file
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    A 'blank slate' config file, appropriate for general use on most high performance
    compute environments. Assumes that all software is installed and available on
    the PATH. Runs in `local` mode - all jobs will be run on the logged in environment.
----------------------------------------------------------------------------------------
*/

process {
    errorStrategy = { task.exitStatus in [143,137,104,134,139] ? 'retry' : 'finish' }
    maxRetries    = 1
    maxErrors     = -1

    // Process-specific resource requirements
    // NOTE - Please try and re-use the labels below as much as possible.
    //        These labels are used and recognised by default in DSL2 files hosted on nf-core/modules.
    //        If possible, it would be nice to keep the same label naming convention when
    //        adding in your local modules too.
    // See https://www.nextflow.io/docs/latest/config.html#config-process-selectors
    withLabel:process_single {
        cpus   = { Math.min( 2 * task.attempt, params.max_cpus as int) }
        memory = { 
            def req = 6.GB * task.attempt
            def max = params.max_memory as nextflow.util.MemoryUnit 
            req > max ? max : req
            }

        time   = {
            def req = 4.h * task.attempt
            def max = params.max_time as nextflow.util.Duration
            req > max ? max : req
            }
        }
    withLabel:process_low {
        cpus   = { Math.min( 1 * task.attempt, params.max_cpus as int) }
        memory = { 
            def req = 12.GB * task.attempt
            def max = params.max_memory as nextflow.util.MemoryUnit 
            req > max ? max : req
            }

        time   = {
            def req = 4.h * task.attempt
            def max = params.max_time as nextflow.util.Duration
            req > max ? max : req
            }
    }
    withLabel:process_medium {
        cpus   = { Math.min( 6 * task.attempt, params.max_cpus as int) }
        memory = { 
            def req = 36.GB * task.attempt
            def max = params.max_memory as nextflow.util.MemoryUnit 
            req > max ? max : req
            }

        time   = {
            def req = 8.h * task.attempt
            def max = params.max_time as nextflow.util.Duration
            req > max ? max : req
            }
    }
    withLabel:process_high {
        cpus   = { Math.min( 10 * task.attempt, params.max_cpus as int) }
        memory = { 
            def req = 72.GB * task.attempt
            def max = params.max_memory as nextflow.util.MemoryUnit 
            req > max ? max : req
            }

        time   = {
            def req = 16.h * task.attempt
            def max = params.max_time as nextflow.util.Duration
            req > max ? max : req
            }
    }
    withLabel:process_long {
        cpus   = { Math.min( 6 * task.attempt, params.max_cpus as int) }
        memory = { 
            def req = 36.GB * task.attempt
            def max = params.max_memory as nextflow.util.MemoryUnit 
            req > max ? max : req
            }

        time   = {
            def req = 20.h * task.attempt
            def max = params.max_time as nextflow.util.Duration
            req > max ? max : req
            }
    }
    withLabel:process_high_memory {
        cpus   = { Math.min( 6 * task.attempt, params.max_cpus as int) }
        memory = { 
            def req = 200.GB * task.attempt
            def max = params.max_memory as nextflow.util.MemoryUnit 
            req > max ? max : req
            }

        time   = {
            def req = 8.h * task.attempt
            def max = params.max_time as nextflow.util.Duration
            req > max ? max : req
            }
    }
    withLabel:error_ignore {
        errorStrategy = 'ignore'
    }
    withLabel:error_retry {
        errorStrategy = 'retry'
        maxRetries    = 2
    }
}
